using UnityEngine;
using System.Collections;

public class GameGUI: MonoBehaviour {
	public GUIStyle style;


	/*
	void OnGUI () {
		GUI.Label (new Rect (10, Screen.height-30, 100, 40), "[W][A][S][D]もしくは[↑][←][↓][→]で移動", style);
		GUI.Label (new Rect (10, Screen.height-55, 100, 40), "左Shiftでダッシュ", style);	

		if (GUI.Button(new Rect (10, Screen.height-90, 100, 30), "リセット")) {
						Application.LoadLevel (Application.loadedLevelName);
				}
	}
	*/
}